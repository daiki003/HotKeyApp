<Window x:Class="HotKeyCommandApp.Views.ButtonCreationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:HotKeyCommandApp.ViewModels"
        xmlns:local="clr-namespace:HotKeyCommandApp.Views"
        Title="{Binding Title}" 
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        PreviewKeyDown="Window_PreviewKeyDown"
        Loaded="Window_Loaded"
        SizeToContent="Manual">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <local:MenuIndicatorConverter x:Key="MenuIndicatorConverter" />
    </Window.Resources>

    <Border Background="#1E1E1E" 
            CornerRadius="16" 
            BorderBrush="#007ACC" 
            BorderThickness="2"
            Margin="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="0" />
        </Border.Effect>
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Text="{Binding Title}" 
                       FontSize="20" FontWeight="Light" Foreground="White"
                       HorizontalAlignment="Center" Margin="0,0,0,15"/>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Input View (Top) -->
                <StackPanel Grid.Row="0" VerticalAlignment="Top" Margin="0,0,0,10">
                    <TextBlock Text="{Binding InputPrompt}" 
                               Foreground="#888888" FontSize="14" 
                               Margin="0,0,0,5" TextWrapping="Wrap"/>

                    <!-- Value Input (URL/Path) -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Border x:Name="InputTextBoxBorder" CornerRadius="4,0,0,4" Background="#2D2D2D">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="1.5" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsItemSelected}" Value="False">
                                            <Setter Property="BorderBrush" Value="#007ACC" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBox x:Name="InputTextBox"
                                     Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                                     Background="Transparent" Foreground="White"
                                     BorderThickness="0"
                                     Padding="10" FontSize="16"
                                     CaretBrush="White"
                                     GotFocus="TextBox_GotFocus"
                                     VerticalContentAlignment="Center">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding CommitInputCommand}" />
                                    <KeyBinding Key="Escape" Command="{Binding CancelInputCommand}" />
                                    <KeyBinding Modifiers="Control" Key="P" Command="{Binding BrowseFileCommand}" />
                                    <KeyBinding Modifiers="Alt" Key="Left" Command="{Binding NavigateBackCommand}" />
                                    <KeyBinding Modifiers="Alt" Key="Right" Command="{Binding NavigateForwardCommand}" />
                                </TextBox.InputBindings>
                            </TextBox>
                        </Border>
                        <Button Grid.Column="1" Command="{Binding BrowseFileCommand}"
                                Visibility="{Binding ShowMainBrowseButton, Converter={StaticResource BooleanToVisibilityConverter}}"
                                Content="📂" Background="#3D3D3D" Foreground="White" 
                                Padding="10,0" FontSize="16" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0,1,1,1" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="0,4,4,0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsItemSelected}" Value="False">
                                            <Setter Property="BorderBrush" Value="#007ACC" />
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#4A4A4A"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>

                        <!-- App Path Input has been replaced purely by ListBox selection -->

                    <!-- Options (Argument / File Search) -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"
                                Margin="0,10,0,0">
                        <CheckBox IsChecked="{Binding IsRequiresArgumentChecked}" 
                                  Visibility="{Binding ShowArgumentOption, Converter={StaticResource BooleanToVisibilityConverter}}"
                                  Foreground="#888888" VerticalAlignment="Center" Margin="0,0,10,0">
                            <TextBlock Text="引数付き [F1]" FontSize="11"/>
                        </CheckBox>
                        <CheckBox IsChecked="{Binding IsFileSearchChecked}" 
                                  Visibility="{Binding ShowFileSearchOption, Converter={StaticResource BooleanToVisibilityConverter}}"
                                  Foreground="#888888" VerticalAlignment="Center" Margin="0,0,5,0">
                            <TextBlock Text="ファイル検索 [F2]" FontSize="11"/>
                        </CheckBox>
                    </StackPanel>
                </StackPanel>

                <ListBox x:Name="CommandListBox"
                         Grid.Row="1"
                         ItemsSource="{Binding DisplayCommands}"
                         SelectedItem="{Binding SelectedItem}"
                         Visibility="{Binding IsListBoxVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="0"
                         ScrollViewer.VerticalScrollBarVisibility="Visible"
                         ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                         HorizontalContentAlignment="Stretch">
                <ListBox.Resources>
                    <Style TargetType="ScrollBar">
                        <Setter Property="Width" Value="3" />
                        <Setter Property="MinWidth" Value="2" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ScrollBar">
                                    <Grid x:Name="Bg" Background="Transparent" SnapsToDevicePixels="true" Width="{TemplateBinding Width}">
                                        <Track x:Name="PART_Track" Orientation="{TemplateBinding Orientation}"
                                               IsDirectionReversed="true" IsEnabled="{TemplateBinding IsEnabled}"
                                               Width="{TemplateBinding Width}">
                                            <Track.Thumb>
                                                <Thumb x:Name="Thumb" Background="#444444" BorderThickness="0"
                                                       MinWidth="2" />
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger SourceName="Thumb" Property="IsMouseOver" Value="true">
                                            <Setter TargetName="Thumb" Property="Background" Value="#666666" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.Resources>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="VerticalContentAlignment" Value="Stretch" />
                        <Setter Property="Width" Value="{Binding DataContext.ButtonWidth, RelativeSource={RelativeSource AncestorType=ListBox}}" />
                        <Setter Property="Height" Value="{Binding DataContext.ButtonHeight, RelativeSource={RelativeSource AncestorType=ListBox}}" />
                        <Setter Property="Margin" Value="0,0,0,8" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="ItemBorder" 
                                            Background="{TemplateBinding Background}" 
                                            CornerRadius="8" 
                                            BorderThickness="1" 
                                            BorderBrush="#444444"
                                            Padding="15,2">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" 
                                                Command="{Binding DataContext.ExecuteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}" />
                                        </Border.InputBindings>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            
                                            <ContentPresenter Grid.Column="0" />
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#3D3D3D" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#4A4A4A" />
                                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#007ACC" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Background="Transparent">
                            <TextBlock Text="{Binding Name}" Margin="5,0,5,0" FontSize="{Binding DataContext.FontSize, RelativeSource={RelativeSource AncestorType=ListBox}}" Foreground="White" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" TextWrapping="Wrap" MaxHeight="{Binding DataContext.ButtonHeight, RelativeSource={RelativeSource AncestorType=ListBox}}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.InputBindings>
                    <KeyBinding Key="Enter" 
                                Command="{Binding DataContext.ExecuteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ListBox}}" />
                    <KeyBinding Key="Down" Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" />
                    <KeyBinding Key="Up" Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" />
                </ListBox.InputBindings>
            </ListBox>
            </Grid>
        </Grid>
    </Border>
</Window>
